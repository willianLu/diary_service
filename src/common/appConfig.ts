export interface appConfigInterface {
    /**
     * server 服务接口定义
     *
     * @type {{
     *         port: number
     *     }}
     * @memberof appConfigInterface
     */
    server: {
        port: number
    }
}